# fit-ease-pwr
Software engineering project.


# Clonar el repositorio a tu ordenadora

git clone https://github.com/EfranFenris/fit-ease-pwr.git
cd fit-ease-pwr/backend   # si el backend está dentro de una carpeta backend

# Ejecutad con Maven Wrapper (no hace falta tener Maven instalado):
chmod +x mvnw
./mvnw spring-boot:run

# Abrir en el navegador:

	•	http://localhost:8080/  → debería salir Hello World!